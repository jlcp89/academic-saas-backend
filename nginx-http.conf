# Configuración HTTP para Academic SaaS
# Este archivo debe ser incluido en el contexto http del nginx.conf principal

# Rate limiting zones
limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=login:10m rate=5r/m; 